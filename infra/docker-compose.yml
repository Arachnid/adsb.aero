name: infra

services:
  postgres:
    build: ./postgres
    image: ghcr.io/arachnid/adsb.aero/postgres:latest
    restart: unless-stopped
    shm_size: 256m
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-adsb}
      POSTGRES_USER: ${POSTGRES_USER:-adsb}
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    networks:
      - db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-adsb} -d $${POSTGRES_DB:-adsb}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --save "" --appendonly no --maxmemory 2gb --maxmemory-policy allkeys-lru
    networks:
      - cache
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ../server
    image: ghcr.io/arachnid/adsb.aero/api:latest
    restart: unless-stopped
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB:-adsb}
      POSTGRES_USER: ${POSTGRES_USER:-adsb}
      LOG_QUERIES: ${LOG_QUERIES:-false}
      REDIS_URL: redis://redis:6379
    secrets:
      - sentry_dsn
    volumes:
      - scheduler_cache:/data/cache
      - terrain_data:/data/terrain
    networks:
      - default
      - db
      - cache
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health')\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  nginx:
    image: ghcr.io/arachnid/adsb.aero/web:latest
    build:
      context: ../web
    restart: unless-stopped
    entrypoint: ["/etc/nginx/entrypoint.sh"]
    command: ["/docker-entrypoint.sh", "nginx", "-g", "daemon off;"]
    volumes:
      - ./nginx/prod.conf.template:/etc/nginx/templates/default.conf.template:ro
      - ./nginx/entrypoint.sh:/etc/nginx/entrypoint.sh:ro
      - openaip_cache:/var/cache/nginx/openaip
    ports:
      - "80:80"
      - "443:443"
    secrets:
      - openaip_api_key
      - origin_cert
      - origin_key
    depends_on:
      - api

  ofelia:
    image: mcuadros/ofelia:latest
    restart: unless-stopped
    entrypoint: ["/bin/sh", "/etc/ofelia/start.sh"]
    secrets:
      - sentry_dsn
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./ofelia/config.ini.template:/etc/ofelia/config.ini.template:ro
      - ./ofelia/start.sh:/etc/ofelia/start.sh:ro
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres_data:
  scheduler_cache:
  terrain_data:
  openaip_cache:

secrets:
  sentry_dsn:
    file: ./secrets/sentry_dsn
  openaip_api_key:
    file: ./secrets/openaip_api_key
  origin_cert:
    file: ./secrets/origin.crt
  origin_key:
    file: ./secrets/origin.key

networks:
  db:
    # Not marked internal: job containers need outbound internet access to download
    # traces from GitHub. Postgres is secured by the absence of port mappings.
  cache:
    internal: true  # Redis has no need for outbound internet access.
